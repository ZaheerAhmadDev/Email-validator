this is  a email validator which check email whether email is valid or not.so forked this and use it free of cost.
other that you need to download redis for large email checking.
i upload all the node module file so you do not need to download the dependencies
